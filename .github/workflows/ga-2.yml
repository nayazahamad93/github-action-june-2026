name: checkout the code

on: workflow_dispatch

jobs:
    checkout:
        runs-on: ubuntu-latest
        steps: 
             
            - name : checkout the repository
              uses: actions/checkoutv7
    
            - name: list the file
              run: ls -lart
